<%= form_with(model: customer) do |form| %>
  <% if customer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
        <% customer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :tin, style: "display: block" %>
    <%= form.text_field :tin %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.text_field :address %>
  </div>

<!---
  <div>
    <%= form.label :movie_id, style: "display: block" %>
    <%= form.text_field :movie_id %>
  </div>
  -->
  
 <div>
    <%= form.label :movie_id, style: "display: block" %>
    <%= form.select :movie_id, options_for_select( Movie.all.pluck( :title, :id)) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<!-- 5 agergando liena 30 a 32 un campo para que muestre el id de movie -->
<!-- Pero el desafio pide seleccionar una pelicula cuando se edite al cliente-->
<!-- Dato el id de la linea 37 es para que se envien los parametros-->
